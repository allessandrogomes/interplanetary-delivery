export default interface IAddressInfo {
    id: string,
    typeAddress: string,
    surnameAddress: string,
    address: string,
    planet: string,
    defaultAddress: boolean,
    isEditing: boolean
}